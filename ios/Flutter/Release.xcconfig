#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
#include "Generated.xcconfig"

// Release matches Profile for MediaKit video paths (same intent as Android
// proguard-android.txt non-optimize). Strip settings live in project.pbxproj /
// Podfile — do not reintroduce aggressive linker stripping here.
